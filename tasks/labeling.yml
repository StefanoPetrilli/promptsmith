version: "3"

tasks:
  setup:
    desc: Pre-download/cache the selected labeling backend model
    cmds:
      - .venv/bin/python -m numeri labeling-setup

  start:
    desc: Auto-label synthetic images into YOLO format (+ optional quality filter)
    cmds:
      - .venv/bin/python -m numeri labeling-start