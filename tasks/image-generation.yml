version: "3"

tasks:
  setup:
    desc: Pre-download/cache the selected generation backend model
    cmds:
      - .venv/bin/python -m numeri generation-setup

  start:
    desc: Generate synthetic images from templated prompts
    cmds:
      - .venv/bin/python -m numeri generation-start